public class Solution {
    // DO NOT MODIFY THE LIST. IT IS READ ONLY
    public ArrayList<Integer> solve(final List<Integer> A, int B) {
        int N = A.size();
        ArrayList<Integer> outArr = new ArrayList<Integer>(N);
        for(int i = 0; i < N; i++)
            outArr.add(A.get(i) + B);
        return outArr;
    }
}
